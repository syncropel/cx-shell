# GitHub API (Starter) Blueprint

This is a minimal blueprint for the public GitHub API, designed to be included with `cx init`.

It provides one action: `getUser`.

This endpoint does not require authentication.
